{
  "name": "ai-agent-course-code",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "clean": "pnpm -r exec rm -rf node_modules dist",
    "init:env": "bash scripts/init-env.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
